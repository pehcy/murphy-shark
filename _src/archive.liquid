---
layout: default
title: Archive - MurphyShark
---

<section class="grid gap">
  <h1 class="big-title">
    ARCHIVE
  </h1>
  {% for post in collections.posts %}
    <article>
      <time>{{ post.data.date }}</time>
      <h2 class="no-margin">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h2>
      <p class="no-margin">{{ post.data.description }}</p>
    </article>
  {% endfor %}
</section>